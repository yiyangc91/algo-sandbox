#include <algorithm>
#include <limits>
#include <vector>

#include "alloc.h"

using namespace std;

namespace {
   int allocateTask(int taskNum, vector<int>& tasks, vector<int>& workers) {
   }
}

int backtrackShortest(vector<int> tasks, int numWorkers) {
   return 0;
}
